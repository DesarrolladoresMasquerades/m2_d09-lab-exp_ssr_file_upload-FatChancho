{{#if user}}
    <img src="{{user.imgUrl}}" alt="" width="100">
    <h2>Welcome, {{ user.username }}!</h2>
    <p>This is your profile page my friend! And your email is: {{ user.email }}</p>
    
    <button><a href="/post">Create a new post</a></button>
    <button><a href="/">See all the posts</a></button>
{{else}}
    <p>Still no logged in user, sorry!</p>
{{/if}}